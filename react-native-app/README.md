# React Native Application

A basic React Native application with intentionally introduced code smells and security issues for PR review testing.

## Running the Project

```bash
npm install
npm start
```

Then press `i` for iOS simulator or `a` for Android emulator.

