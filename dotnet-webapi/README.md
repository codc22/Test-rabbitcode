# .NET WebAPI Project

A basic WebAPI project with intentionally introduced code smells and security issues for PR review testing.

## Running the Project

```bash
dotnet restore
dotnet run
```

The API will be available at `https://localhost:5001` or `http://localhost:5000`

